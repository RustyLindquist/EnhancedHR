import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Expert Access — EnhancedHR.ai',
    description: 'Apply to become an expert instructor on EnhancedHR.ai. Build and monetize your HR expertise.',
};

export default function JoinExpertLayout({ children }: { children: React.ReactNode }) {
    return children;
}
